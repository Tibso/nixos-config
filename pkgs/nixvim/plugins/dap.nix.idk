{ pkgs, ... }: {
  programs.nixvim.plugins = {
    dap = {
      enable = true;

      adapters = {
        executables = { lldb = { command = "${pkgs.lldb}/bin/lldb-dap"; }; };
      };
    };
    #dap-ui.enable = true;
    #dap-virtual-text.enable = true;
  };
}
