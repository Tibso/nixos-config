{
  programs.nvf = {
    enable = true;
    enableManpages = true;

    settings.vim = {
      viAlias = false;
      vimAlias = true;

      statusline.lualine = {
        enable = true;
      };
      telescope.enable = true;
      autocomplete.nvim-cmp.enable = true;

      languages = {
        enableLSP = true;
        enableTreesitter = true;

        nix.enable = true;
        rust.enable = true;
      };
    };
  };
}
